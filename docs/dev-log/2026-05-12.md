# 2026-05-12 dev-log

记录本日对 team-standards 的**决策型变更**——5 次规则方向反转 + 1 次基础设施重构(H1-H4 优化)。

## 一、`coding-standards-common §5.0` 规则方向反转:注释语言"沟通语言一票否决"

**原版**: §5.0 默认"沟通语言写注释",但留了"存量文件已是另一种语言 → 沿袭"的退路。

**新版**: 取消存量豁免——中文沟通的会话里新增注释一律中文,短期内单文件中英混杂可接受,后续重构再统一。唯一覆盖默认的合法路径是用户在本会话明确要求特定语言。

**为什么改**: 用户实测发现 AI 在中文会话里继续往英文文件追加英文注释,规则被退路架空。

**对应 commit**: `ca27694`(2026-05-09)

---

## 二、`architecture-ddd-lite-fullstack` 新增「Service 业务动作扩展强制阈值」

**原版**: "新代码落点决策"只兜底"巨型方法 / 旧骨架文件"场景,AI 看到 200-300 行 service 不命中就放行。

**新版**: 新增「Service 业务动作扩展」专节,T1-T6 六条强制拆分阈值——新状态机 / 新下游 / 新事务边界 / 新前置校验 / 既有 ≥3 个 public / 主方法 > 80 行,命中任一必须拆子 service。

**为什么改**: 用户提出场景——OrderService 已有 refund/cancel/reject,加 reverseCheckout 时 AI 惯性追加成第 4 个 public 方法,违反单一职责。需要更显式的拆分判定。

**对应 commit**: `c41fe40`

---

## 三、规则方向反转:T1-T6 阈值 → 「新业务分支 = 新子 service」铁律

**原版**(2 小时前):T1-T6 六条阈值 + 8 条豁免清单。

**新版**: 取消阈值与豁免框架,回到一句话原则——只要新增的是新业务分支就必须拆,无阈值无豁免。T1-T4 从"拆分阈值"降级为"识别新分支的信号",T5/T6 删除。

**为什么改**: T5/T6 + 8 条豁免清单实际成了 AI 凑判断的退路;用户指出"机械的一动作一子 service 也比留判断空间好"。

**对应 commit**: `067276e`

---

## 四、规则方向反转:取消"同分支变种"对 god service 的豁免

**原版**(上一小时):允许同分支参数化变种(如 `refund` 与 `partialRefund`)留在原 service。

**新版**: 取消唯一例外——同分支变种也不进 god service,要么进该分支的 focused service,要么本次扩展就是把既有方法 + 变种一并迁出 god service 的契机。god service 最多 1 行 delegate。

**为什么改**: 用户指出"共享前置状态机的情况也应抽到一个 service 里包装成两个函数",不该回到 god service。

**对应 commit**: `b28d335`

---

## 五、`architecture-ddd-lite-fullstack` 补 4 个缺口

**新增内容**:
- 跨分支编排:同回合 ≥2 focused service 走独立 `XxxOrchestrator` / `XxxSaga`,O1-O4 触发条件
- 横切关注点豁免:日志 / 审计 / 权限 / 事务 / metrics 走 AOP/拦截器,横切实现类不算 god service
- 服务命名 taxonomy:项目级选定 `Service` / `UseCase` / `CommandHandler` 一种贯彻;禁 `XxxApplicationService` / `XxxManager`
- 聚合边界 5 问:一事务一聚合根,跨聚合走 Domain Event/Saga;模糊默认独立聚合

**为什么改**: 用户评审 plugin 时识别出"每个业务分支一 focused service"规则落地后,跨动作编排 / 横切重复 / 命名混用 / 聚合边界 4 个缺口需要补。

**对应 commit**: `333e4f9`

---

## 六、`architecture-ddd-lite-fullstack` 把 Python / Dart 与 Java 拉齐

**新增内容**: frontmatter 显式声明 Python(FastAPI/Django/Flask)和 Dart(Serverpod/Shelf)同等触发;横切关注点表 3 栈对照;反模式代码示例 Java/Python/Dart 三栈写法;命名 taxonomy 跨语言项目原则;新增 Python 后端约束 + Dart 后端约束章节。

**为什么改**: 用户在同一仓库同时开发 Java/Python/Dart,原 SKILL 偏 Java/Spring 让其它栈开发者感觉不适用。

**对应 commit**: `3c50e33`

---

## 七、Plugin H1/H2/H3/H4 基础设施重构(本次 dev-log 记录的重点)

**评审定位**: 用户要求评审整个 plugin 设计后给出 H1-H4 高优先级问题:索引三处重复维护 / 24 skill 认知开销高 / frontmatter 持续膨胀 / hook 无测试 + 无 Node 引擎声明。

**优化**:

1. **H1 索引单源化**: 新增 `scripts/sync-agents.js`(CLAUDE.md → AGENTS.md 派生 + `--check` CI 校验);README.md 24 行 skill 表改为 8 阶段紧凑列表
2. **H2 分类导航**: CLAUDE.md 顶部加 8 组分类导航 + 「选 skill 决策助手」速记
3. **H3 描述压缩**: 6 个最严重 skill 的 frontmatter 压缩 45%-76%(backend-kg 2764→660 等),完整决策树留在 SKILL.md body
4. **H4 hook 测试 + CI**: 新增 hooks/package.json(engines.node ≥18) + 19 个端到端测试 + GitHub Actions(Linux/macOS/Windows × Node 18/20/22 矩阵 + AGENTS.md 同步校验)

**为什么改**: plugin 已经到了"复杂度自身成为维护负担"阶段——本次会话已经多次"改 1 个 skill 要同步 3 处文件"的痛点实证。下一阶段不再加新 skill,而是合并/抽象/测试。

**对应 commit**: `9d3c1d7`

---

## 八、Plugin M1/M2/M3/M4/M5 中优先级优化(本次会话末尾)

**新增**:

1. **M1 跨引用校验**: `scripts/check-cross-refs.js` 校验所有 SKILL.md / CLAUDE.md / README.md 内的跨 skill 引用 + 跨章节「」引用,被引文件改章节名后引用失效会被 CI 检出
2. **M2 并发触发顺序**: CLAUDE.md 新增「Skill 并发触发顺序与冲突解决」节,21 步全局调用顺序 + 9 条冲突解决规则 + 异常中断处理
3. **M3 元数据补全**: 新增 `CHANGELOG.md`(关键版本变化)+ `docs/skill-dependencies.md`(skill 间前置/后置/互补/互斥关系图),弥补 plugin.json 不支持声明 skill 依赖
4. **M4 README selling pitch**: 顶部加 30 秒 TL;DR + 解决什么问题 + 5 分钟试用 quickstart
5. **M5 dev-log 补回**: 写本文件,把近期 5 次规则方向反转 + 基础设施重构集中沉淀

**为什么写**: 评审报告里 M5 指出"4 次规则方向反转都没写 dev-log",skill 自己定义的规则被自己绕过。本文件就是补回。

---

## 决策原则沉淀(给未来 AI / 维护者读)

1. **规则方向反转 = 必写 dev-log**——不只是 commit body。本次会话证明:连续反转 4 次时 commit body 已经写过的内容,从全局视角看不到"一连串决策路径",必须由 dev-log 沉淀。
2. **AI 凡是"还能写得更详细"的退路都会用到**——T5/T6 阈值 + 8 条豁免清单立刻被 AI 当成"凑判断"的工具。规则越简单越短,执行率越高。
3. **基础设施(脚本 / hook / CI)永远比文档约束可靠**——CLAUDE.md 写"必须同步 AGENTS.md"被忽视了多轮,直到加了 sync-agents.js + CI 校验才彻底解决。后续遇到"这条规则反复违反"的场景,优先考虑写脚本/hook 兜底而非加更多文字。
4. **架构原则跨语言基本一致,差异在机制实现**——focused service / 聚合边界 / 横切关注点都不分语言,差的只是 `@Transactional` vs `with session.begin():` vs `db.transaction()`。
