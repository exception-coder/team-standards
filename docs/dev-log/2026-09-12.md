# 开发日志 · 2026-09-12

## 16:00 · 调整流程 · OpenSpec 文档推进与证据检查

**原因：** 工件存在、会话里提到 change 或任务被勾选，均不能证明本次实施有明确设计依据和有效验证。文档推进需要可执行的阶段条件、意见回写、切片归属和恢复入口。

**改动：**

- 复用 change-readiness 与 delivery-verification，加入阶段准出、概设详设、评审职责、暂停取消和交接契约，不新增同义 Skill。
- 增加共享 JSON 检查器及会话绑定、初始脏范围保护、场景/视图/任务映射、代码与文档指纹、显式证据记录和独立 CI 检查。
- 新增 Stop 适配以任务累计两次补齐为上限；默认报告试运行，block 模式替换 OpenSpec 的旧启发式分支。官方 CLI 继续负责工件解析、同步和归档。
- 增加真实 Git、OpenSpec 1.13.0、路径、失效和失败测试；两项原有测试统一文本换行及 Windows 临时目录表示，保持原断言。
- 跨仓发布契约按 LF 规范化文本比较，与存量完整性指纹一致；避免三个仓库的 Git 换行配置差异造成内容相同却无法打包。

**影响：**

- 项目不复制团队规则，不增加必需治理配置；机器记录引用现有正文，不能伪装人工批准。
- 混合文件归属、外部 planning store、多 change 聚合、无 change 的 S 档机械豁免和真实业务宿主验收仍有明确支持边界，不能将本次交付宣传为全场景强制闭环。
- 未启用 OpenSpec 的源码仓库沿用兼容设计流程；本仓开发依据为用户知识库中的实施方案与编码摘要。

## 状态契约治理与实际执行证据

**原因：** 样衣状态写入已变更而消费者仍判断旧值，旧夹具又与错误实现一致。单纯增加 AI 声明不能阻止交付。

**改动：** 在插件中实现可分发的契约 Schema、来源绑定、候选图谱、无 shell 的验证执行和输入新鲜度检查；四个既有 Skill 渐进路由，现有治理入口检查已登记模块。

**决策：** 业务语义归项目，插件不内置项目状态。选用 JSON 以保持 Node 18 无第三方运行依赖；不改 Graphify 原始图，候选边不冒充业务事实。先逐模块接入，不对未登记代码作保护承诺。

**限制：** 字面扫描不完备，测试质量、外部环境及旧制品需项目验证。未安装到运行中的会话缓存，也未开启任何业务仓远端分支保护。

**验证：** Windows / Node 24 本地原生验证：最终 hooks 测试 171 项通过、1 项跳过；随后使用独立 OpenSpec 1.13.0 补跑真实 CLI 用例，1 项通过、无跳过。12 项状态治理用例包含旧 ACTIVE 故障注入、遗漏消费者、过期证据、执行期间输入变化及命令失败。Schema 示例执行通过，跨 Skill 引用、版本同步、Agent 同步、Skill 审计与套件镜像契约检查通过。

**图谱与交付：** 执行 graphify update 刷新源码图，3116 节点；少量 JSON 元数据无节点，未冒充语义完整。源指纹检查通过。本轮未使用不可用的 forge_verify，未修改服务器 HTTP 接口或数据库。业务项目试点仅以隔离样衣场景验证机制，未宣称已接入 Yoooni One 真实业务模块或其他成员宿主。

## 17:55 · 调整流程 · AI 原生文档同步与完成后自动提交

**原因：** 用户要求套件每次更新同步文档，并在完成作业后自动 commit，避免文档漂移与改动积压。旧规则仅允许 team-standards 自动提交，业务项目还要求重复确认；英文 README 的能力数量也已落后。

**改动：** 文档同步归 markdown-writing-standards，自动提交归 git-commit-standards，change-readiness 与 delivery-verification 衔接完成流程；同步中英文 README、Agent 入口、流程及索引，版本更新至 2.7.0。

**影响：** 适用规范的套件和业务项目默认完成后本地提交；只暂存本次范围，保留无关改动，明确禁止和人工确认约束仍有效。自动 commit 不自动授予业务 push 权限，保留 team-standards 源码自动 push 特例。纯文档走文档验证，无改动不空提交，验证失败不得声明完成。

**边界：** 本次修改 Skill 工作流程，没有新增强制 Hook，不能声称已在所有成员宿主生效。套件根目录不是 Git 仓库，总览同步仅为本地文件，不计入组件提交。

**验证：** 使用项目原生检查，四个已修改 Skill 的 quick_validate、跨引用、22 Skill 审计、Agent 同步、三处 2.7.0 版本一致性与升版检查、入口文档本地链接检查通过。hooks 全量测试首轮 170 通过、1 跳过、1 个隔离 Codex 安装因 Windows 临时拒绝访问失败；未改源码或关闭检查，单独重跑该安装用例后 1 通过。真实 OpenSpec CLI 用例未配置，因此仍跳过。Graphify 执行代码图更新并刷新指纹；未将其宣称为文档语义重建。

## 18:06 · 调整流程 · Bug 证据必需、独立报告按风险生成

**原因：** 用户指出强制 Bug 文档缺少实际价值且拖慢迭代。旧入口以目录、固定章节和图表为中心，代码定位和工作日志又依赖报告存在，削弱了排障与回归的主线。

**改动：** 保留 bug-doc-required 调用名，收敛到证据、因果判断、最小修复与回归；简单 Bug 不建报告，复杂或高风险问题复用共享载体。模板变为按需参考，修复编码规范引用 common；解除定位和日志的文档前置条件，项目 docs/bug/ 允许保存确需的分析。

**影响：** 文档位置 Hook 仅增加项目 docs/bug/ 例外，其他新建文档限制保留。现有 OpenSpec、验证及提交要求不降低；无修复的调查不创建空提交。版本升至 2.8.0，不重命名 Skill，避免破坏其他入口。套件总览仅本地同步，根目录仍不属于 Git 仓库。

**验证：** 项目原生 hooks 回归 177 项：176 通过、0 失败、1 项真实 OpenSpec CLI 用例因未配置跳过。新增 5 项测试覆盖 Write / apply_patch 新建共享 Bug 记录、其他目录和路径跳转仍阻断、已有文档仍可编辑；隔离安装测试通过。Skill 格式、跨引用、版本与 Agent 同步、入口链接和图谱源指纹检查通过。代码图已更新，未声称重建所有文档语义；本次未配置业务 Runtime，未声称业务事故场景已验证。
