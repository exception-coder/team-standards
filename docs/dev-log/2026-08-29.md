# 2026-08-29 开发日志

## 01:51 · 调整流程 · AI 工程结构初始化

**原因：** 项目上下文已经收敛到项目规则、长期知识、OpenSpec、Graphify、项目能力和实现代码六类权威来源，但新项目仍缺少一个可重复、可验证的物理初始化入口，容易再次出现目录随意、入口不一致或重复事实文档。

**改动：**

- 在 `init-project-docs` 增加 `structure` 模式与确定性初始化脚本，不新增平行公共 Skill。
- 以 Yoooni One 的已验证结构提炼通用模板，但移除其 Java、React、Oracle 和业务模块专属内容。
- 初始化最小 Agent 入口、文档索引、AI 结构说明、OpenSpec 入口和 Graphify 提交边界。
- 已有文件只读保护，可选目录按需创建，Graphify 与 OpenSpec 仍由各自工具负责。

**影响：**

- 后续新项目默认使用同一套上下文检索顺序和事实源边界。
- 执行初始化 Skill 可直接作用于当前目录，并可安全重复执行。
- 项目特有编码规范和业务知识仍必须由项目自身声明，不进入公共模板。
