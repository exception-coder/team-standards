{
  "_comment": "Hook 配置，默认未启用。如需启用，将下方 _disabled_PreToolUse 改为 PreToolUse。",
  "hooks": {},
  "_disabled_PreToolUse": [
    {
      "matcher": "Write|Edit|MultiEdit",
      "hooks": [
        {
          "type": "command",
          "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/check-design-doc.cmd\"",
          "async": false
        }
      ]
    }
  ]
}
