# Rejected Reference Metadata

Use the canonical template at `references/reference-metadata-template.md`. Set status to `rejected`, record observable problems and reason, and link a replacement when one exists.
