# 编码纠正与项目规则

用户指出规范错误时，先核实具体差异并修正本次实现。业务需求变化、设计取舍和普通修改请求不自动算“违规”。

可复用的项目约束优先合并到已有项目 Agent 入口或规则正文，去重并说明适用范围；一次纠正不自动升级为跨项目规则。不为每次反馈新建违规报告、累计次数或追加历史叙事。

项目明确维护 `docs/coding-violations.md` 时沿用该文件，仅阅读与任务相关的条目并按已有约定更新，不默认每次编码全量读取、不强制新建、不删除历史记录。纯咨询反馈不自动获得修改项目规则的授权。
