# 实施检查

## 复用判断

重复业务规则属于重复知识，应收敛到单一来源；偶然相似但会独立演化的流程不应强行抽象。编码前先查项目 coding profile、公共模块和已有能力清单。

## 外部契约

调用外部 API 或陌生库前核对当前依赖版本、官方接口和项目封装。无法验证时明确标记假设，不编造方法、字段或错误码。

## 全局名称

新增路由、事件名、依赖注入键、插件 ID、数据库约束名或配置键前全局检索，确认没有同名和语义冲突。

## 异常

禁止空 catch、无日志吞错和用默认值伪装失败。包装异常时保留原始 cause 和业务上下文；边界层将内部错误映射为稳定契约。

