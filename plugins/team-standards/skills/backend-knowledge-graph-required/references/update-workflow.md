# 图谱更新工作流

## 编码前

确认涉及表、读写顺序、过滤条件、状态决策和已有原子能力。信息缺失时先调查，不能靠命名猜测。

统计、筛选、分页或批量查询先执行 [高风险查询性能门禁](query-performance-gate.md)。命中风险时先回显候选规模、最坏调用数、优化方案和验证证据，不直接进入编码。

## 编码中

优先复用正式登记的能力和 SQL。发现能力边界不准确时记录证据并修正文档，不复制一个近似实现。

## 编码后

把新事实写入候选池，包含时间、问题、证据位置、建议归属和置信度。同一主题候选合并更新。验证稳定后，合并到正式卡片并更新 `00_index.md`。

## SQL 归档

同用途 SQL 合并为一张卡片，保留参数化主版本；变体只记录差异条件。禁止保存包含真实凭据或敏感数据的 SQL 示例。

高风险或高频 SQL 同时归档数据规模、调用频率、调用次数、执行计划摘要、实测耗时和已设置的保护边界。
