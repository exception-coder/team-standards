# 编码定位（旧模板入口）

独立编码摘要已取消。旧入口仅保留迁移说明，不再用于生成 `-coding.md`。

实施前用新鲜 Graphify 和定向源码确认文件、符号、消费者及约束；必要定位直接写入现有任务。设计决策留在唯一设计载体，方法实现留在代码，不复制逐方法伪代码或行号目录。

见 [设计文档工作流](references/document-workflow.md)。历史摘要仍可作为待核实资料读取，不自动删除。
