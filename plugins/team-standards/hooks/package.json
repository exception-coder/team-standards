{
  "name": "team-standards-hooks",
  "private": true,
  "version": "0.1.0",
  "description": "team-standards plugin 的 PreToolUse hook 脚本（跨平台 Node，无第三方依赖）",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "test:performance": "node benchmarks/check-hook-performance.js",
    "test:full": "node --test",
    "test:fast": "node --test tests/change-input.test.js tests/contract-integrity.test.js tests/hook-event-contract.test.js"
  }
}
